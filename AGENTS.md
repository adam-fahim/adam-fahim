# AGENTS.md

## Cursor Cloud specific instructions

This is a **GitHub profile README repository** (`adam-fahim/adam-fahim`). The repository contains only a single `README.md` file that is rendered on the owner's GitHub profile page.

- **No application code, services, or dependencies** exist in this repo.
- **No build, lint, or test commands** are available — there are no `package.json`, `Makefile`, `requirements.txt`, or similar config files.
- Changes to this repo are limited to editing `README.md` (Markdown).
- To verify changes, render the Markdown locally (e.g. `python3 -m markdown README.md` or view in a Markdown previewer) or push and check the GitHub profile page.
